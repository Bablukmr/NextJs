export default function Layout({children}){
    return(
        <div>
        <h1 style={{color:'green'}}>Comman Layout For Login Screen</h1>
        {
            children
        }
        </div>
    )
}